apiVersion: v1
kind: Service
metadata:
  name: backend-dev
  namespace: simple-chat-dev
  labels:
    app: backend
    environment: development
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: backend
    environment: development
