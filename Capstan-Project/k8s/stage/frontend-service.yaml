apiVersion: v1
kind: Service
metadata:
  name: frontend-stage
  namespace: simple-chat-stage
  labels:
    app: frontend
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: frontend
    environment: staging
