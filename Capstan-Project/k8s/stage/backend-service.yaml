apiVersion: v1
kind: Service
metadata:
  name: backend-stage
  namespace: simple-chat-stage
  labels:
    app: backend
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: backend
    environment: staging
