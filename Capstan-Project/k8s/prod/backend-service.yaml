apiVersion: v1
kind: Service
metadata:
  name: backend-prod
  namespace: simple-chat-prod
  labels:
    app: backend
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: backend
    environment: production
