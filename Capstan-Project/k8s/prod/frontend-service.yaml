apiVersion: v1
kind: Service
metadata:
  name: frontend-prod
  namespace: simple-chat-prod
  labels:
    app: frontend
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: frontend
    environment: production
