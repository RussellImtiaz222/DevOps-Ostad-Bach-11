name: Frontend Production Pipeline

on:
  push:
    branches:
      - prod
    paths:
      - 'simpleChatui/**'
      - '.github/workflows/frontend-prod.yml'
  pull_request:
    branches:
      - prod
    paths:
      - 'simpleChatui/**'

env:
  REGISTRY: ghcr.io
  FRONTEND_IMAGE: ${{ github.repository }}/simple-chat-frontend

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./simpleChatui
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: './simpleChatui/yarn.lock'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Type check
        run: yarn build

      - name: Lint
        run: yarn lint

      - name: Generate version
        id: version
        run: |
          VERSION="v$(cat package.json | grep version | head -1 | awk -F: '{ print $2 }' | sed 's/[",]//g' | tr -d ' ')-$(git rev-parse --short HEAD)"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "timestamp=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_OUTPUT

      - name: Log in to Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push production image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v5
        with:
          context: ./simpleChatui
          file: ./simpleChatui/Dockerfile.prod
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE }}:prod-${{ steps.version.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE }}:prod-latest
            ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE }}:${{ steps.version.outputs.version }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.version.outputs.timestamp }}
            org.opencontainers.image.environment=production

  deploy-to-prod:
    needs: build-and-test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: production
    
    steps:
      - uses: actions/checkout@v4

      - name: Trigger Production Deployment (Automatic)
        env:
          ARGOCD_AUTH_TOKEN: ${{ secrets.ARGOCD_AUTH_TOKEN }}
        run: |
          echo "🚀 AUTOMATIC DEPLOYMENT TRIGGERED"
          echo ""
          echo "✓ Build and tests passed"
          echo "✓ Production image pushed: ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE }}:prod-latest"
          echo ""
          echo "📋 Argo CD is now syncing production environment"
          echo "   Application: frontend-prod"
          echo "   Namespace: simple-chat-prod"
          echo ""
          echo "⏱️  Rolling update in progress (watch progress in Argo CD dashboard)"
          echo ""
          # Optional: Uncomment to integrate with Argo CD API
          # curl -X POST https://argocd-server/api/v1/applications/frontend-prod/sync \
          #   -H "Authorization: Bearer $ARGOCD_AUTH_TOKEN" \
          #   -H "Content-Type: application/json"
